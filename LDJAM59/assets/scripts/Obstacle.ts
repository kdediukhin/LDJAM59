import { _decorator, Component } from 'cc';
const { ccclass } = _decorator;

/** Маркер-компонент. Луч останавливается при попадании в этот объект. */
@ccclass('Obstacle')
export class Obstacle extends Component {

    
}
